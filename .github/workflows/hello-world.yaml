name: Hello World
on:
  push:
    branches: [master]
jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - run: echo "TODAY=$(date +%d-%m-%y)" >> $GITHUB_ENV
      - run: echo Hello World, today is $TODAY